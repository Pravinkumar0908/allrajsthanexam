import 'package:flutter/material.dart';

Widget createWebIframe(String url) {
  return const Center(
    child: Text(
      'PDF preview is loading in app view...',
      style: TextStyle(fontWeight: FontWeight.bold),
    ),
  );
}
